repositories:
  - owner: raphaelmansuy
    name: edgequake
    sync: true           # auto-sync issues on startup
  - owner: matrixorigin
    name: matrixflow
    sync: true # onboard only, skip issue sync (too many issues)
