# MatrixOne 连接诊断报告

**诊断时间**: 2026-02-21 14:18:37  
**当前公网IP**: 49.87.13.180

## 📊 测试结果汇总

### ✅ 成功的测试
1. **DNS 解析**: ✅ 成功
   - 主机名: `019c3caf-ae5a-7ced-ac5b-37d070f153f5.db.moi.matrixorigin.cn`
   - 解析到IP: `47.96.137.127`
   - 实际服务器: `alb-oa5jcvy15wpy6g8i04.cn-hangzhou.alb.aliyuncs.com` (阿里云负载均衡器)

2. **Ping 测试**: ✅ 成功
   - 平均延迟: 33.579ms
   - 丢包率: 0%

3. **连接字符串格式**: ✅ 正确
   - 格式: `mysql+pymysql://admin:****@019c3caf-ae5a-7ced-ac5b-37d070f153f5.db.moi.matrixorigin.cn:6001/github_issues`

4. **数据库引擎创建**: ✅ 成功
   - SQLAlchemy 引擎创建正常

### ❌ 失败的测试
1. **端口连通性**: ❌ 失败
   - 端口: `6001`
   - 错误码: `35` (连接超时)
   - 测试IP: `47.96.137.127:6001`
   - 测试主机名: `019c3caf-ae5a-7ced-ac5b-37d070f153f5.db.moi.matrixorigin.cn:6001`

2. **数据库连接**: ❌ 失败
   - 错误代码: `2003`
   - 错误信息: `Can't connect to MySQL server on '...' (timed out)`
   - 所有后续测试都因为无法连接而失败

## 🔍 问题分析

### 主要问题
**端口 6001 无法连接，导致数据库连接超时**

### 可能的原因

#### 1. 🔒 IP 白名单限制（最可能）
MatrixOne Intelligence 服务可能配置了 IP 白名单，只允许特定 IP 访问。

**解决方案**:
- 登录 MatrixOne Intelligence 控制台
- 找到实例 "实例_260208测试"
- 在安全设置中添加当前公网IP: `49.87.13.180`
- 或者添加 IP 段: `49.87.13.0/24`

#### 2. 🚧 防火墙规则
服务器防火墙可能阻止了 6001 端口的访问。

**解决方案**:
- 检查 MatrixOne 实例的安全组规则
- 确认 6001 端口是否对外开放
- 检查是否需要配置入站规则

#### 3. 🔌 端口配置错误
可能实例使用的不是 6001 端口。

**解决方案**:
- 在 MatrixOne Intelligence 控制台查看正确的连接端口
- 检查连接信息中显示的端口号
- 更新 `config/config.py` 中的 `MATRIXONE_PORT` 配置

#### 4. 🌐 VPN 要求
某些 MatrixOne 实例可能要求通过 VPN 访问。

**解决方案**:
- 确认是否需要连接 VPN
- 如果需要，先连接 VPN 后再测试

#### 5. 📍 网络路由问题
虽然可以 ping 通，但特定端口可能被路由策略阻止。

**解决方案**:
- 检查网络路由配置
- 联系 MatrixOne 技术支持

## 🛠️ 建议的解决步骤

### 步骤 1: 检查 MatrixOne Intelligence 控制台
1. 登录 MatrixOne Intelligence 控制台
2. 找到实例 "实例_260208测试"
3. 查看连接信息，确认：
   - ✅ 主机地址是否正确
   - ✅ 端口号是否正确（可能是 6001 或其他端口）
   - ✅ 是否有 IP 白名单设置

### 步骤 2: 配置 IP 白名单
1. 在实例的安全设置中
2. 添加当前公网IP: `49.87.13.180`
3. 或者添加 IP 段（如果IP会变化）

### 步骤 3: 验证端口
如果控制台显示的不是 6001 端口，更新配置文件：
```python
# config/config.py
MATRIXONE_PORT = os.getenv("MATRIXONE_PORT", "正确的端口号")
```

### 步骤 4: 重新测试
运行诊断脚本验证连接：
```bash
python3 diagnose_connection.py
```

## 📝 当前配置信息

```python
MATRIXONE_HOST = "019c3caf-ae5a-7ced-ac5b-37d070f153f5.db.moi.matrixorigin.cn"
MATRIXONE_PORT = "6001"
MATRIXONE_USER = "admin"
MATRIXONE_DATABASE = "github_issues"
```

## 🔗 相关文件

- 诊断脚本: `diagnose_connection.py`
- 连接测试: `test_matrixone_connection.py`
- 配置文件: `config/config.py`
- 连接示例: `example_pymysql_connection.py`

## 💡 下一步行动

1. **立即行动**: 检查 MatrixOne Intelligence 控制台的 IP 白名单设置
2. **添加IP**: 将 `49.87.13.180` 添加到白名单
3. **验证端口**: 确认控制台显示的端口号是否为 6001
4. **重新测试**: 添加白名单后，重新运行 `diagnose_connection.py`

---

**注意**: 如果问题仍然存在，建议：
- 联系 MatrixOne 技术支持
- 提供此诊断报告
- 说明已尝试的解决方案
