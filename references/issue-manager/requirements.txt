# GitHub Issue 智能管理系统 - 依赖包列表

# HTTP请求
httpx>=0.25.2

# 数据库（MatrixOne使用MySQL协议，需要pymysql和sqlalchemy）
sqlalchemy>=2.0.23
pymysql>=1.1.0  # MySQL/MatrixOne支持（必需）
cryptography>=3.4.8  # pymysql的依赖，用于SSL连接（必需）
# psycopg2-binary>=2.9.9  # PostgreSQL支持（如果使用PostgreSQL才需要）

# AI服务（支持多AI提供商，优先千问，失败时回退到Claude）
# OpenAI（可选）
# openai>=1.3.5  # 如果使用OpenAI才需要

# Claude (必需，作为备用AI服务)
anthropic>=0.18.0

# 通义千问 (必需，优先使用)
dashscope>=1.17.0

# 其他工具
python-dotenv>=1.0.0
