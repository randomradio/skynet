{
  "name": "@skynet/agent-runtime",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"No lint configured for @skynet/agent-runtime yet\"",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "format": "echo \"No format task configured for @skynet/agent-runtime yet\"",
    "clean": "rm -rf dist coverage"
  },
  "dependencies": {
    "@skynet/sdk": "workspace:*",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.20.5",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
